require 'toPinyin'


#let's sort it 
words = "
检查
我们
的二
进制
是否
适合
你的
平台".split("\n")

words.sort! {|a ,b|   a.pinyin.join <=> b.pinyin.join }

sorted = "
的二
检查
进制
你的
平台
适合
是否
我们".split("\n")

raise "sort failed" if words.join != sorted.join


see ./lib/toPinyin/test.rb for more usage.
